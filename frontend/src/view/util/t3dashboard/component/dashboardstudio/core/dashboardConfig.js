const config = {
  use_ai: true,
  previewRowLimit: 100,
  tableTopN: 1000,
  maxDataSources: 5,
  defaultMergeType: 'LEFT_JOIN',
  autoSave: true,
  defaultTableColumnAlign: 'left',
  defaultTableColumnWidth: 120,
};

export default config;
